---
title: Embedding core
description: Environment, runtime, instance, layers and policies, values.
sidebar:
  order: 2
---

The four execution levels and what each owns; how a host declares its contract as a port layer, feeds values, and reads outputs; the instance as the front door - five observables and four commands. This page will follow one host from a bare runtime to a deployed program.
