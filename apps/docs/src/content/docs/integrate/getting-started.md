---
title: Getting started
description: Install core, create a language, run a program.
sidebar:
  order: 1
---

`createStdlib()`, `createEnvironment(language)`, a program, `run`. Four lines from install to a value. This page will be the shortest complete embedding, with the next pages taking each part further.
