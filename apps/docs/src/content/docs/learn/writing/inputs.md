---
title: "Inputs"
description: "$name reads a value from outside."
sidebar:
  order: 2
---

`$score` is an input: a value the host - or the pane beside the editor - supplies. A program declares the inputs it reads; a value change recomputes only what depends on it. This page will add inputs to the running example and show the recompute happen.
