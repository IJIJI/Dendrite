---
title: Syntax
description: let, output, $inputs, lambdas, operators and comments.
sidebar:
  order: 1
---

A program is a set of `let` bindings and `output`s - no statements, no sequencing, no mutation. `$name` reads an input; `x => …` is a lambda; operators are sugar over ops. This page will be the complete syntax, each form with a live example.
