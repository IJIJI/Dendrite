---
title: "@dendrite-lang/editor"
description: "The code editor: a headless core and React components, mounted over a connection."
sidebar:
  order: 2
---

The editor mounts over a connection - its own stack, a runtime the host runs, or an instance the host already has - and releases only what it made. This page will show each connection kind, the React components, the panes and theming.
