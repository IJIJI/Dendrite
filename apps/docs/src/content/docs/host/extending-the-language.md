---
title: "Extending the language"
description: "Types, ops, evaluators, operators, and inference."
sidebar:
  order: 3
---

Register a type (with fields, an extends, a schema); register an op and its evaluator; add an operator as sugar; refine types at analysis time with `inferOutput` and `inferInputTypes`; pick which stdlib segments to build on. This page will build one host-specific op end to end.
