//? The editor's React sample opens with `import "@dendrite-lang/editor/style.css"`, which is
// true and worth showing. Astro's own generated types cover a CSS import, but they live in
// .astro/, which is gitignored and absent when the test suite runs on its own in CI.
declare module "*.css";
