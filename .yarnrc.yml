# Plain node_modules install rather than Yarn PnP: the workspace hosts Vite/React apps
# whose toolchains resolve modules themselves, and every package shares this one install.
nodeLinker: node-modules
