//? @dendrite-lang/editor — public API (framework-free).
//
// The editor's headless core: the compile/run session, the CodeMirror adapter, the
// document model + stores, and createEditor for hosts. The React UI lives under the
// ./react subpath and is the only place React is allowed (see .docs/editor-plan.md).

export {};
