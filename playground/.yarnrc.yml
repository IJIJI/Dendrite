# The playground is a standalone nested project: the repo root is Yarn PnP, but Vite's
# toolchain is happiest with a plain node_modules tree, so this project uses its own
# linker + lockfile. The presence of yarn.lock here makes Yarn treat this dir as its
# own project root (independent of the repo root's PnP install).
nodeLinker: node-modules
